{
  "name": "0xclaw",
  "private": true,
  "version": "0.1.0",
  "description": "OpenClaw crypto compatibility kit",
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "0xclaw": "packages/cli/src/index.mjs"
  },
  "scripts": {
    "build": "echo 'build: placeholder'",
    "test": "echo 'test: placeholder'",
    "lint": "echo 'lint: placeholder'"
  },
  "engines": {
    "node": ">=20"
  }
}
